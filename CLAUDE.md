# このプロジェクトについて

Claudeを使って、自分専用の小さな道具（暮らし・趣味用）を作っていくプロジェクトです。仕事用のツールではありません。詳しい経緯や考え方は [handoff.md](handoff.md) にまとまっているので、迷ったらそちらも読んでください。

ユーザーはエンジニアではありません（土木工学専攻・数値解析研究の大学院生）。説明・コメントはすべて日本語で、専門用語（例：リポジトリ＝変更履歴を記録する場所、コミット＝変更内容を記録として保存すること、デプロイ＝ネット上に公開すること）を使う場合は、必ず一言で言い換えを添えてください。

# 絶対に守ること

- **無料であること最優先。** 有料API、有料ホスティング、月額サブスクは一切使わないでください。
- 少しでも料金が発生しそうな設定・サービスを提案するときは、実装する前に必ずユーザーに確認してください。
- 説明はすべて日本語、専門用語を避けるか噛み砕いてください。

# 道具の作り方の原則

- **1つの道具＝1枚の自己完結HTMLファイル。** HTML・CSS・JavaScriptを1ファイルにまとめ、ビルド作業（コードを変換・結合する準備作業）やサーバーが不要な形にします。
- 設定値（入力した数値や選んだ項目など）は、URLの後ろについているパラメータ（`?key=value`のような部分）に埋め込みます。そうすると、そのURLをブックマークするだけで「自分専用に設定済みの道具」になります。
- 端末をまたいだ同期は考えません（ブラウザに保存する`localStorage`という仕組みは、その端末の中にしか残らないためです）。
- 入力・出力は「コピー&ペースト」で完結する形を基本にします。

# 新しい道具を追加する手順

1. アイデアを聞いたら、まず下の「アイデアを評価する基準」に照らして作る価値があるか確認する（当てはまらなければ率直に伝える）。
2. [tools/_template.html](tools/_template.html) をコピーして、`tools/名前-ランダムな文字列.html` を作る。日本語の名前は画面のタイトル部分に書けばよい。ランダムな文字列は `python3 -c "import secrets,string; print(''.join(secrets.choice(string.ascii_lowercase+string.digits) for _ in range(12)))"` で作る（このファイルに実際のファイル名を書き残さないこと）。
3. 道具のロジックを、そのHTMLファイル1つの中にJavaScriptとして実装する。外部の部品（ライブラリ）を使うのは構わないが、有料サービスや会員登録が必要なAPIは使わない。
4. できあがったら、道具のHTMLの`<head>`に `<meta name="robots" content="noindex, nofollow, noarchive">` を入れる。**[index.html](index.html) やREADMEには道具へのリンクを書かない**（一覧から辿れてしまうと、ファイル名をランダムにした意味がなくなるため）。できあがったURLはユーザーに直接伝える。
5. 変更を記録として保存する（gitのコミット）ときは、やり方をユーザーに確認しながら進める。記録のメッセージは日本語で簡潔に書く。
6. このファイルの「今温めているアイデア」の状態（未着手→完成、など）を更新する。

# アイデアを評価する基準

**却下するもの：**
- 検索すればすぐ出てくるもの
- 既存アプリですでに解決済みのもの（既存アプリを自分向けに使いやすくするのはあり）
- 自分で判断できることを、代わりに判断させるだけのもの

**価値があるもの：**
- 量が多すぎて人の手作業では無理なもの
- 自分では気づけない、自分の癖やパターンを映し出すもの
- 毎回やっている地味な作業を消してくれるもの
- 他人には価値がなくても、自分には毎回必要なもの

（詳しい説明は [handoff.md](handoff.md) の第5章）

# 今温めているアイデア

優先度はつけていません。どれから着手してもよい一案集です（7-2だけは7-1と土台が共通なので、7-1を先に作ってから乗せます）。詳細は [handoff.md](handoff.md) の第7章を参照してください。

| 番号 | 内容 | 状態 |
|---|---|---|
| 7-1 | 通学バスツール：桂駅西口⇄桂御陵坂。回数券が使える京阪京都交通の便だけを絞り込んで「あと何分で出るか」を表示 | 完成（[tools/bus.html](tools/bus.html)） |
| 7-2 | 通勤路線の異常時通知（7-1と土台が共通）：平常時は通知なし、異常時のみ一行通知 | 未着手（7-1が土台） |
| 7-3 | ライフログ日記：カレンダー・写真から日記を自動生成 | 保留・じっくり検討中 |
| 7-4 | 過去の記録を横断して自分の癖を出すツール | 保留・じっくり検討中 |

# 困ったときは

- git（変更履歴を記録する仕組み）の操作や、GitHub Pages（無料の公開場所）への公開作業など、専門的な判断が絡む場面では、選択肢をいきなり複数出さずに、ユーザーに確認しながら進めてください。
- gitの運用はシンプルに保ちます。複雑な枝分かれ（ブランチ）は作らず、`main`という1本の記録に直接積み重ねていく形にしてください。
