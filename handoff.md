# プロジェクト引き継ぎ資料：Claudeで作る暮らしの道具

Claude.ai（チャット）でのアイデア出しから、Claude Codeでの実装フェーズに移すための引き継ぎ資料。

---

## 1. このプロジェクトの目的

Claudeを使って、自分専用の小さな道具を作る。仕事用ではなく、暮らし・趣味の中で毎日使うツール。

## 2. 作る人について

- 修士課程2年生（土木工学専攻・数値解析研究）
- コーディング経験あり：C++、Python、Fortran（研究用途）
- ツール・ソリューションはすべて無料で（有料API・ホスティング・サブスク不可）
- 説明は日本語、専門用語を避けて噛み砕いて

## 3. 採用している方向性：「1枚のHTMLファイルで完結する道具」

Simon Willisonという開発者が実践しているスタイルを参考にしている。HTML + JavaScript + CSSを1ファイルにまとめ、ビルド工程もサーバーもなしで動く小さな道具を、思いつくたびに1個ずつ作って並べていくというやり方。彼はこれを200個以上作って公開している（tools.simonwillison.net）。

**この方向性を選んだ理由：**
- インストール・アカウント登録・月額料金が不要
- 一度作ればそのまま動き続ける（ランニングコストがかからない）
- **Claudeの役割は最初に一度組み立てることだけ。そのあと毎日動かす必要がない**（これは結構重視）
- 気軽に作って、ダメなら捨てられる（低リスク）

**実装上の工夫（Simon Willisonのパターンより）：**
- 状態（設定値など）はURLのクエリパラメータに埋め込む → そのURLをブックマークすれば「自分専用の設定済みツール」になる
- 端末をまたいだ同期が必要ない設計にする（localStorageは端末内にしか残らないため）
- ホスティングはGitHub Pagesなど静的ホスティングが向いている（無料、HTMLを置くだけで公開できる）
- 入出力は「コピー&ペースト」が一番使いやすい
- iOSならホーム画面に追加してアプリのように開ける

## 4. 道具の3分類

並べてみると、この3種類にほぼ収束する。

1. **変換**：形式Aで持っているものを形式Bにする（例：表のコピペをCSVに変換、画像から文字を読み取る）
2. **計算**：数字を入れると答えが出る（例：通勤ツール）
3. **確認**：目に見えないものを見えるようにする（例：写真の撮影情報を表示する）

## 5. アイデアの判断基準

**却下するもの：**
- 検索すればすぐ出てくるもの
- 既存アプリですでに解決済みのもの(既存アプリをより自分に合ったもにブラッシュアップするのはあり)
- 自分で判断できることを、代わりに判断させるだけのもの

**価値があるもの：**
- 量が多すぎて人間には手作業で無理なもの（例：過去の記録を横断して自分の癖を出す）
- 自分では見えない、自分のパターンを映し出すもの
- 毎回やっている地味な詰め替え作業を消すもの
- 他人には価値がないが、自分には毎回必要なもの


## 7. 温めているアイデア一覧

### 7-1. 通勤ツール
徒歩時間（家→駅、駅→勤務地）と時刻表を固定で入力しておき、開くと「次の電車に乗るには、あと何分で家を出ればいいか」だけを一発で表示する。

- 出発時刻・到着時刻より「あと何分」の残り時間表示を主役にする
- 候補は次の1〜2本まで。3本目以降は結局選ぶ作業になるので出さない
- 発展案：始業時刻を入れておき、「1本遅らせても間に合うか」の余裕時間を出す
- 実装イメージ：Claude.aiのVisualizer機能で試作したインタラクティブなプロトタイプが本チャットにあり（HTML/JS）。同じロジックをiOSショートカット、または1枚のHTMLファイル（GitHub Pages等でホスティング、設定はURLパラメータに埋め込み）に落とし込む想定
- 設定を固定した版がベースで、時刻表を書き写す手間だけ払えば動く。動的に必要なのは遅延情報のみ

### 7-2. 通勤路線の異常時通知（7-1と土台が共通）
平常時は通知なし。遅延など異常があったときだけ、「中央線遅延、+10分、東西線経由に変更」のような一行だけ届く。

- データ取得の選択肢：
  - 公共交通オープンデータセンター（ODPT）：開発者登録無料、REST/JSON。参加事業者に偏りがあるため自分の路線が対象か要確認
  - 自前で経路検索エンジンを組む（重め）
  - 固定経路なら時刻表を手動で.mdに書いておき、動的に取るのは遅延情報だけにする（最小構成でおすすめ）
- Yahoo!乗換案内などのスクレイピングは規約上グレーなので避ける
- これは7-1と違い、**毎日Claude（または何らかの監視処理）が動く必要がある**部分

### 7-3. ライフログ日記（保留・じっくり検討したい案）
毎朝、前日のカレンダー・音声メモ・その日iPhoneで撮った写真から日記を自動生成し、gitに積んでいく。

- 写真はEXIFの撮影時刻・位置情報が使え、「実際どこにいたか」の記録になる。カレンダーとの突き合わせで「予定と実際のズレ」も拾える
- 写真取り込み経路：iOSショートカットで毎晩書き出し（手軽）、またはosxphotos（Mac母艦がある場合、`--export-by-date`で日付フォルダに書き出し）
- 写真の選別ルールを先に決める：スクショ・レシート等は除外、連写は1枚に畳む、その日を代表する3〜5枚に絞る。原寸は使わず縮小版のみgitに入れる
- **本体は「自分の口調と思考パターンを写したスキル」（`skills/me/SKILL.md`のようなもの）を作る部分**。日記はその出力先に過ぎない
- 事実ベースの記述に限定し、感情の推測を書かせない
- 発展案：「1年前の今日」を毎朝の出力に足す、月末に写真4〜6枚で1ページのHTMLレポートを作る

### 7-4. 過去の記録を横断して自分の癖を出すツール
会議録音・日記・チャットログなど大量の記録をClaudeに読ませ、自分では気づけないパターン（同じ論点で毎回つまずく、対立を避けて話をぼかす場面など）を抽出させる。7-3の「自分を写すスキル」作りとも関連が深い。

## 8. 次にやること

1. **道具の置き場所を用意する**（GitHub Pagesなど無料の静的ホスティング）。「思いついたらHTMLファイルを1枚足すだけ」の状態にしておくと、以降のアイデア実行コストが下がる
2. 第7章のアイデア（7-1〜7-4）はどれから着手してもよい。優先度は上下をつけず、その時々の気分や状況で選ぶ
   - ただし、7-2（異常時通知）は7-1（通勤ツール）と土台が共通なので、7-1を先に作ってから乗せるのが自然
   - 7-3（ライフログ日記）と7-4（自分の癖を出すツール）は保留・じっくり検討したい案であり、思いついたらすぐ着手する種類のものではない

## 9. Claude Codeへの申し送り事項

- 説明は日本語、専門用語は避けるか噛み砕いて説明してほしい（ユーザーはエンジニアではない）
- 無料であることが絶対条件（有料API、有料ホスティング不可）
- 第7章のアイデアは判断基準（第5章）に沿って評価・実装してほしい
